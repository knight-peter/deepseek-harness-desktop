# dsh-desktop 开发环境配置（提交进仓库）
#
# 加载顺序（后覆盖前）：.env → .env.development → .env.local → .env.development.local
# 本文件放「开发环境」才需要的覆盖值（团队共享），例如开发模式下指向
# 上游源码的 DSH_CHECKOUT 约定路径。本机个性化/私有配置放 .env.local 或
# .env.development.local（均被 gitignore）。
#
# 示例：
# DSH_CHECKOUT=/path/to/deepseek-harness

# ── 开发运行期开关（`pnpm dev` 通过 scripts/dev.mjs 加载，仅 dev 生效）──

# 与「已安装的发布版 dsh-desktop」并存时，用独立 userData 避免抢单实例锁
# （开发版拿到锁失败会直接退出）：
DSH_DESKTOP_USER_DATA=/tmp/dsh-desktop-dev-userdata

# 启动后自动打开管理窗口（插件/调试/设置），方便直接看插件页：
DSH_DESKTOP_OPEN_MANAGER=1

